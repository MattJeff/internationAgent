{
  "mcpServers": {
    "siglair": {
      "type": "http",
      "url": "${user_config.base_url}/v1/mcp/server",
      "headers": {
        "Authorization": "Bearer ${user_config.cle_api}"
      }
    }
  }
}
